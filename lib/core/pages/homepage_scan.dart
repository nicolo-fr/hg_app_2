import 'package:flutter/material.dart';

class HomepageScan extends StatelessWidget {
  const HomepageScan({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('scan'),
    );
  }
}